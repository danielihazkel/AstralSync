/**
 * Version of the interpretation content library a snapshot was written
 * against (PRD §6, `content_version`). No library exists until Phase 1f,
 * so every snapshot records "0" for now.
 */
export const CONTENT_VERSION = "0";
